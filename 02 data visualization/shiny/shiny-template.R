
# loading packages and data --------------------------------------------------------------------------------------------------------------
library('shiny')

# UI -------------------------------------------------------------------------------------------------------------------------------------
ui <- fluidPage(
  
  ## App title ---------------------------------
  titlePanel(
    title = 'This is title'
  ),
  
  ## sidebar layout with a input and output ----
  sidebarLayout(
    
    ### input ----------------------------------
    sidebarPanel(
      
    ),
    
    ### output ---------------------------------
    mainPanel(
      
    )
  )
)

# SERVER ---------------------------------------------------------------------------------------------------------------------------------
server <- function(input, output){
  
}

# run shinyApp ---------------------------------------------------------------------------------------------------------------------------
shinyApp(ui, server)
